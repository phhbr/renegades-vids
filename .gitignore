assets
.venv/
__pycache__/
*.pyc
analysis/
plays/
*.npy

# models/ is deliberately NOT ignored: the YOLO weights are vendored so that
# Ultralytics never tries to auto-download them at a venue with no internet.
